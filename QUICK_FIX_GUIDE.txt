===========================================
QUICK FIX GUIDE - ADD CATEGORY FEATURE
===========================================

STEP 1: BACKUP
--------------
1. Go to: C:\xampp\htdocs\lineseiki.systems-test.com\application\controllers\
2. Copy cms.php to cms.php.backup

STEP 2: OPEN CMS.PHP
--------------------
1. Open: C:\xampp\htdocs\lineseiki.systems-test.com\application\controllers\cms.php
2. Find the function: public function add_category()
3. It should be around line 627-671

STEP 3: REPLACE THE FUNCTION
-----------------------------
DELETE the entire add_category() function and REPLACE with the code from:
ADD_CATEGORY_FIXED_CODE.php

STEP 4: SAVE THE FILE
---------------------
Save cms.php

STEP 5: TEST
------------
1. Open browser
2. Go to: http://localhost/lineseiki.systems-test.com/cms/products
3. Click "Add New Category"
4. Fill in Category Name: "Test Category"
5. Upload a test image (JPG or PNG)
6. Click "Add Category"

EXPECTED RESULT:
- Green success message appears
- Page reloads
- New category appears in the grid

IF IT STILL DOESN'T WORK:
-------------------------

1. CHECK BROWSER CONSOLE (F12)
   - Look for JavaScript errors in Console tab
   - Check Network tab for the add_category request
   - Look at the response - it should show JSON

2. CHECK PHP ERROR LOG
   Location: C:\xampp\apache\logs\error.log
   Look for any PHP errors

3. CHECK FOLDER PERMISSIONS
   Make sure C:\xampp\htdocs\lineseiki.systems-test.com\assets_system\images\
   is writable (755 or 777 permissions)

4. CHECK DATABASE
   - Open phpMyAdmin
   - Database: lineseiki_db  
   - Table: tbl_product_category
   - Check if table has these columns:
     * id
     * category_name
     * product_image
     * slug
     * display_order
     * is_active
     * created_at
     * updated_at

5. ENABLE DEBUG MODE
   Add these lines at the top of cms.php (after <?php):
   
   error_reporting(E_ALL);
   ini_set('display_errors', 1);

COMMON ISSUES:
--------------

Issue: "Image upload failed"
Fix: Check if assets_system/images folder exists and is writable

Issue: Form submits but nothing happens  
Fix: Check browser console for JavaScript errors

Issue: "Failed to add to database"
Fix: Check database connection in config/database.php

Issue: Page refreshes but category doesn't appear
Fix: Check if category was actually inserted - go to phpMyAdmin and check tbl_product_category table

KUNG AYAW PA RIN:
-----------------
Send me the following:
1. Browser console errors (F12 > Console tab)
2. Network request/response (F12 > Network > add_category)
3. PHP error log content
4. Screenshot of the error message

===========================================
